# -*- coding: utf-8 -*-
"""Heart disease.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/15pyVxuV2Cjg7oZkNhiXK7mTrTjE48emX
"""

import pandas as pd
import numpy as np
import io
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import accuracy_score
from google.colab import files
uploaded = files.upload()

filename = list(uploaded.keys())[0]
# Use the dynamically obtained filename
dataset = pd.read_csv(io.BytesIO(uploaded[filename]))

dataset.head()

#rows columns
dataset.shape

dataset.info()

x= dataset.drop(columns='target', axis=1)
y= dataset['target']
print(x,y)

dataset.isnull().sum()

x= dataset.drop(columns='target', axis=1)
y= dataset['target']
print(x,y)

X_train,X_test,Y_train,Y_test=train_test_split(x,y,test_size=0.2,stratify=y,random_state=2)

print(X_train.shape)

model= LogisticRegression(max_iter=10000)
model.fit(X_train,Y_train)
print(model.get_params())

ans=model.predict(X_train)
ans

a=accuracy_score(ans,Y_train)

a

